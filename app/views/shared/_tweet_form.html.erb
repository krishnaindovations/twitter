<div class="tweet-box">
  <%= form_with model: current_user.tweets.new do |form| %>
    <div class="field">
      <%= form.text_area :content, placeholder: "What's happening?" %>
    </div>
    <%= form.submit "Tweet", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
